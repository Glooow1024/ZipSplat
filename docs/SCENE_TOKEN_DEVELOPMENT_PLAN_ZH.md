# ZipSplat 场景 token 开发计划

日期：2026-09-07。目标分支：`dev/scene_token`。

本计划记录本次讨论确定的方向，是后续开发入口。按用户要求使用原单一工作区；2026-09-07已完成P0/P1预实验、P2实现及P3数据pilot/200步初步训练检查，训练链路通过但高质量小样本拟合未通过，未启动大规模训练。执行顺序为 P0 → P1 → P2 → P3 → P4；P5 为后续研究。参数中标为“初始设置”的部分应在首次运行前写入配置并固定，调整时记录原因与新实验版本，不能事后挑选有利口径。

## 1. 工作区、基线和边界

| 项目 | 路径/状态 |
|---|---|
| 本地开发工作区 | `F:/Development/multiview/ZipSplat`； |
| 分支来源 | `dev/multiview` → `dev/scene_token` |
| 创建基线 | `c674892ae8787c83c8f6980ec1c8bdc308c524ee` |
| ToMe 历史工作 | 已提交到 `dev/local_tome` 保存；不带入 `dev/scene_token` |
| 远端现有仓库 | `vllm1:/root/zipsplat/ZipSplat`；检查时 dev/multiview，HEAD 与上述基线一致、工作树干净 |
| 远端开发工作区 | 复用 `/root/zipsplat/ZipSplat`，已从dev/multiview创建并检出dev/scene_token；不新增worktree |
| ZipSplat Python | `/root/zipsplat/ZipSplat/.venv/bin/python`；部署后确认导入来自新代码，而非旧 editable 安装 |
| GVC1D 参考 | `/root/gvc1d_ws/GVC1D`，参考版本 `efdff7992c7cd6fbe76b05d3a5762b77d57e929b` |

ToMe 方向已弃用，不从 `dev/local_tome` 派生，不把未提交 ToMe 文件复制进新工作区。旧工作已提交到历史分支保存。已按后续授权完成原模型预实验、场景token实现与200步初测；本轮修改未提交/推送。本计划作为后续工作的执行依据，不意味着自动启动所有阶段。

## 2. 研究目标与不能混淆的结论

目标：在 DA3 编码中加入每视角 S 个可学习场景槽位，只把指定层的场景特征送入几何解码，研究质量、表示冗余与预算的关系。

- 第一版全程保留原 patch，只在输出时提取场景槽位。这是学习式几何输出瓶颈，不减少 DA3 编码 FLOPs；序列变长可能增加耗时与训练显存。
- 原 ZipSplat k-means 减少 query 和高斯数，几何 CA 仍读取全部 patch 特征。它的压缩率曲线不是新场景表示的容量极限，也不是严格上下界。
- 第一版保留独立原图颜色 KV。因此不能声称只存场景 token 就能离线独立解码整个场景。
- 固定每视角 S 时总量为 VS，仍随视角数增长，不等于固定全场景预算或自动消除跨视角冗余。
- GVC1D 提供学习式 latent 读取思路；视频条件编码、空间分块 global 模块不直接移植为 DA3 跨视角注意力。

## 3. 记号与公平预算

默认图像 252×252、patch14，P=324；DA3-Giant D=1536。V 为视角数，S 为每视角场景槽位数，K 为解码 query 数，G 为高斯总数。

原模型保留比例 r（不是移除比例）：

`K = min(VP, max(1, floor(rVP)))`，`G = 32K`。

新模型首版关闭第二次 k-means：`K=VS`，`G=32VS`。之后若研究二次选择，则使用单独参数 `scene_query_ratio`，不得与原始 patch 保留比例混淆。

同预算对照须匹配实际 K 和 G。原模型 r≈S/P，但必须验证整数取整；必要时在实验层提供明确的 query 数覆盖项，保证选择算法和其余前向不变。不能只比较相同数值的 r。

## 4. P0：固定评测和可复现基线

### 4.1 输入与数据划分

- 主线使用 NoPrior：重建只接收 context RGB，不传 GT 内外参；GT 仅按明确协议用于监督或配准/渲染。
- 优先复用 short60 已有10场景的 context 清单；初始完整矩阵使用共同有效9场景，排除既有缺GT的06。不得重新随机抽样冒充同一输入。
- 初始 V 为4/8/12/16。对每个场景固定8个 held-out target，优先从前60帧中、所有 context 集合的并集之外均匀选取。必须先核验帧图像、GT和预处理；不足8个时在运行前确定替代集合并版本化，不能运行后补选。
- 输入重建指标与 held-out 指标分开。target 不进入重建的图像、特征或缓存。
- 后续训练集来自 DL3DV-10K 有效清单；核验与全部验证/测试场景无重复，按场景划分并冻结清单。交接中10485完整/96异常属于历史检查结果，运行前重新核验。
- 9场景用于预实验和开发选参，不据此宣称最终泛化；正式结果需保留未参与选参的场景集合。

### 4.2 相机、图像和指标

- 保存原图到模型/渲染图的裁剪缩放、内参转换、OpenGL/OpenCV约定、首视角归一化和尺度配准。
- 初始新实验采用252×252评测图像，PSNR/SSIM/LPIPS-Alex v0.1；核对实现、输入范围与聚合方式后写入配置。同一次比较不得混用256尺寸旧报告或VGG版本LPIPS。
- 相机配准方案A：由 r=1 基线在 context 上确定一次配准与相机，跨比例复用，作为固定相机敏感性对照。若压缩引起输出坐标系漂移，该对照会同时包含漂移影响，必须披露。
- 方案B：各比例使用相同初值、步数和损失的独立配准，作为固定预算配准后的质量对照。优先使用 context 做共享坐标对齐，再渲染 held-out；若必须用 target RGB 优化其位姿，单列“target RGB pose-refined”结果，不能标成未经适配的NVS。
- P0须核验A/B可执行性并固定主报告协议；方案A与B的结果不得混成一条曲线。现有30步context配准不自动等于held-out评测流程。
- 汇报逐场景均值与跨场景等权均值，失败/跳过显式列出；不让帧数不同的场景隐式改变权重。

### 4.3 环境和复现

记录源码HEAD及未提交差异、checkpoint路径/SHA256、配置、数据manifest、软件/CUDA/GPU、dtype、随机种子和实际导入路径。原模型保持eval、固定checkpoint，不训练、不做高斯TTO。

P0验收：清单无context/target交叉，GT与预处理检查通过；r=1可重复重建；相机与指标协议完整；原代码基线与新实验封装结果一致。未通过前不启动全矩阵。

## 5. P1：原始 ZipSplat k-means 压缩率预实验

### 5.1 问题和矩阵

只改变k-means保留比例，测原模型在不同query/高斯预算下的质量退化与耗时，不改变backbone、颜色路径、每token高斯数或权重。

| 步骤 | 初始设置 | 目的 |
|---|---|---|
| 冒烟 | 1场景、8视角、r=1与1/8 | 验证索引、计数、输出和指标 |
| 小规模粗扫 | 3场景×8/16视角×6比例，共36组 | 覆盖低纹理、细节丰富、遮挡/大视差；运行前写明场景ID与选择依据 |
| 完整粗扫 | 9场景×4/8/12/16视角×6比例，共216组 | 复用已完成且配置完全一致的子集，不重复计数 |
| 拐点细扫 | 在相邻粗扫点之间插值 | 同一规则选择区间；完整披露，不仅保留最佳点 |
| 固定K对照 | 选所有V都可支持的公共K，跨V比较 | 分开观察观测量与高斯预算影响 |

六个保留比例：`1, 1/2, 1/4, 1/8, 1/16, 1/32`。若到1/32仍未显著退化，可扩展更低预算，但将其标为训练分布外压力测试，不能保证单调性。

优先依据r=1的相对变化报告曲线；初始质量预算采用PSNR下降≤0.5/1.0/2.0dB三个档位，分别给出该档的最小实测K，同时列出SSIM与LPIPS变化及视觉缺陷。这些是报告分档，不是声称统一可接受阈值；后续工作点在看训练/验证结果后明确记录，最终测试不参与选择。

### 5.2 计时、缓存和稳定性

- 分开记录预处理、backbone、k-means、几何/颜色解码、高斯head、配准、渲染，以及端到端wall time和峰值显存。
- GPU计时同步边界，固定warmup和重复次数；初始为3次warmup、10次计时取中位数，记录离散程度和设备占用。质量输出不必重复导出十次。
- 可以缓存原backbone特征加速质量扫参，但缓存键必须含模型/图像/预处理/dtype/层号；缓存运行耗时不能当成完整推理耗时。端到端性能另行跑完整前向。
- 查明k-means初始化是否随机；若随机，对代表场景与拐点用3个种子复测。记录实际K、必要时记录唯一nearest索引数，以识别重复选择。
- 缺文件/GT或运行失败时记录具体原因，不能静默忽略或覆盖已有结果。

### 5.3 产物和验收

拟用独立结果根 `/root/multiview_compare/experiments/scene_token/kmeans_sweep/<run_id>/`，报告根 `/root/multiview_compare/reports/scene_token/<run_id>/`；部署时检查目录再登记，当前尚未创建。

每次保存配置与版本、输入manifest、逐组K/G/指标/耗时、渲染对照和失败表。报告至少包含逐场景曲线、均值曲线、固定K跨V对照、细节裁剪与最小预算分档。CSV/JSON保存明细，Markdown解释结论；PLY/视频按代表点保存，不能覆盖旧short60。

P1验收：所有计划组均有结果或明确失败记录；实际预算和指标口径核对通过；能给出后续S的候选区间。结论写成“原解码器预算曲线”，不得写成“场景只能/至少由S个token表示”。若发生非单调结果先检查选择稳定性、相机漂移和失败点，不强行平滑成单调曲线。

## 6. P2：场景 token 首版结构

### 6.1 Backbone 与多层接口

- 从完整ZipSplat checkpoint初始化，保留其DA3适配权重；不只用原版DA3覆盖主干。
- 新增共享初始化参数 `[S,1536]`，展开为 `[B,V,S,1536]`，前向状态独立。槽位随机初始化以打破对称性，不创建按训练图/视角ID记忆的参数。
- 原patch全程保留，注意力层数、宽度、头数和原局部/全局调度保持；所有槽位参与相应注意力。首版不加入ToMe、熵编码或自适应预算。
- 原patch先完成绝对位置插值，再插入场景槽位；不能把S计入二维网格。首版场景RoPE采用特殊token零位置，以不同学习槽位向量区分身份；此为设计假设，记录并验证。
- 保留camera索引0与block13注入；明确camera/register/scene/patch的切片和位置数组。global普通patch的RoPE在当前实现中不是唯一视角ID。
- 输出19/29/39对应local18/global19、local28/global29、local38/global39；每组场景特征 `[B,V,S,3072]`，沿用归一化/prepare成为 `[B,V,S,1536]`。
- 三层保持相同槽位顺序。反转后的融合次序仍为39→29→19。K=VS，几何CA的KV仅含场景槽位，不得不经说明继续读取稠密几何patch。

### 6.2 颜色分支与高斯head

原颜色特征来自独立可学习 `Conv2d(3,128,kernel=14,stride=14)`，每图324个patch；不是DA3降维或RGB均值。

```text
几何CA/SA融合后的 scene [B,K,1536]
      ├──────────────────────────────┐
      ↓ 新 Linear(1536,128)          │
颜色 query [B,K,128]                 │
      ↓ 原完整 Color CA block        │
      ↑ 原图颜色 KV [B,VP,128]       │
颜色输出 [B,K,128] ───── concat ─────┘
                  [B,K,1664]
                       ↓ Gaussian head
                  G=32K 个高斯
```

- 新增query投影，保留128维color CA、1664维head输入与每token32个高斯，尽量复用权重。
- 严禁把场景nearest_idx直接gather原图颜色patch：即使不越界，也会取错视角/位置。
- Color CA保留原归一化、残差、MLP语义。新增投影与颜色CA需要适配；原颜色KV缺少显式位置/视角编码，重复纹理、曝光/反光混合是待验证风险。
- 首版不取消颜色skip，避免同时改变head输入形状。颜色必须进入压缩瓶颈、位置/视角编码和显式匹配留作后续消融。

### 6.3 代码落点与兼容性

训练实现：[网络](../splatfactory/models/networks/zipsplat.py)、[DA3编码封装](../splatfactory/models/encoders/dav3_encoder.py)、[DA3主干](../splatfactory/models/encoders/dav3_dino.py)、[模型配置](../splatfactory/configs/model/zipsplat.yaml)、[训练器](../splatfactory/trainer.py)。

独立推理实现：[网络](../zipsplat/zipsplat.py)、[主干](../zipsplat/backbone.py)、[模块](../zipsplat/modules.py)、[公开封装](../zipsplat/predictor.py)。两条路径需要配置、checkpoint命名与数值对照，不能只改推理包却使用未改的训练模型。

首版配置显式记录 `scene_tokens_enabled`、`num_scene_tokens`、输出层、位置策略、颜色query来源；禁用时执行原路径。checkpoint记录结构配置，拒绝静默丢失关键权重；S变化或新增模块导致的missing/unexpected keys须明确列出。原公开predictor带no_grad，仅用于推理，训练走可微的核心模型/训练入口。

### 6.4 P2验收测试

- 禁用新功能时与基线数值等价；新增token初始化为零不构成等价保证。
- 验证V=1/2/8/16、候选S下所有层、位置数组、输出形状与32K计数；不同S代表不同配置，不能假设任意截断有效。
- 验证camera注入、三层local/global来源和颜色query不再复用patch索引；用可追踪的构造输入检查索引语义。
- 检查新token、query投影和计划解冻参数有有限梯度；冻结decoder用requires_grad=False而非no_grad，验证上游梯度仍存在。
- 检查训练/推理加载一致，实际导入当前单一工作区；CPU形状测试与服务器小模型CUDA FP16/BF16冒烟后，再做真实checkpoint验证。
- 仅通过形状/梯度测试不能宣称重建有效；P3必须验证训练和held-out效果。

## 7. P3：分阶段训练与 S 选择

2026-09-07预算更新：用户查看预实验后明确认为**保留75%（不是移除75%）**的视觉退化可以接受，将其作为后续S选取依据。该档B-held-out平均PSNR为22.206，相对r=1下降0.307dB；SSIM为0.7164、LPIPS为0.1884。它是开发集上的经验参考，不是逐场景质量保证。

用户随后明确选择**S=256**并授权实现，取代此前S=243的候选。S256对应原模型约79.01%的保留率；同K/G对照需补测，不能直接拿已有r=0.75当作严格匹配。后续按验证质量逐步尝试S=162、81，对应保留50%、25%的已有对照；不默认同时启动全部训练。原32/64/128保留为后续扩展或低成本接口冒烟配置，不再把S=64当作已获预实验支持的保守主预算。

| 每视角S | 对应原模型保留率 | 16视角K | 16视角高斯数 | 用途 |
|---|---:|---:|---:|---|
| 256 | 约79.01% | 4096 | 131072 | 用户指定默认值；P2已实现 |
| 162 | 50% | 2592 | 82944 | 训练成功后的较低预算对照 |
| 81 | 25% | 1296 | 41472 | 更强压缩候选，是否采用由验证决定 |

这里仅匹配解码query/高斯数：原k-means的几何CA仍可访问全部VP特征，场景token版的几何KV只有VS，因此不能承诺S=256也只下降0.307dB；学习式聚合是否能弥补信息减少，必须训练验证。原k-means跨视角全局选点，未保证每视角恰有固定数量的query；此处是总量对应，不能当成同一特征选择。

首版在DA3全程保留原patch并额外加入S个槽位，S=256使每视角patch+scene从324增至580（另有既有特殊token），会增加编码和反向计算/显存。进入完整实验前，以真实checkpoint和训练精度完成该S的显存/前后向profile，再固定batch等配置；不能把75%的输出保留率理解为编码计算量降低25%。若资源不足，记录调整原因，不静默更换主预算。当前已实现用户指定S256，并明确记录79.01%的对应预算。

每个S使用对应训练配置和相近更新预算，不把直接截断已训练槽位当成公平S对照。最终选择依据独立验证集的质量/成本；9个预实验场景已用于选参，不能再作为未参与选参的最终测试集。

1. 小数据过拟合检查：验证端到端损失确实下降，排除坐标、渲染、梯度和颜色路径错误；该结果不能作为泛化证据。
2. 热身：冻结DA3已有参数，训练新场景token、颜色投影以及下游prepare、几何CA/SA、颜色CA、高斯head。冻结DA3仍需为场景token保留梯度路径。
3. 联合适配：逐步解冻DA3后部，再根据验证集决定是否全量微调。分组学习率，新参数通常高于预训练参数；具体值、batch、步数在P3启动配置中确定并记录，不臆定硬件可承受规模。
4. 按固定验证协议比较S，不用最终测试集选参。永久冻结decoder仅作为消融，不作为主方案成立前提。

沿用并审计现有训练入口的渲染/几何监督、尺度正则和相机口径。2026-09-07用户确定监督采样跟随官方实现：训练用原bounded sampler、4个target，允许部分target索引同时被选为context；不得额外将target分支图像/特征送入encoder。固定持出验证/测试则要求context与target互斥。初版不引入GAN来掩盖几何误差。

观察：输入/held-out指标、槽位特征相似度与有效秩、梯度、opacity分布、高斯覆盖、细小结构和重复区域。槽位相似本身不等于无效，结合重建与预算消融判断。特征蒸馏不能把VS与VP按索引逐点MSE；若引入蒸馏，优先固定相机下渲染目标或明确的匹配/聚合机制。

P3验收：有可复现实验配置、训练曲线与未参与训练场景的结果；至少一个S稳定训练且无显著接口故障。若失败，先定位颜色/槽位塌缩/监督问题，不能只加大S或把冻结decoder的失败直接解释为研究方向无效。

## 8. P4：同预算评估与决策

核心对照：原ZipSplat同K/G、新模型对应S；另列原r=1参考。视角/图像/相机/指标保持P0协议一致。记录整网耗时，即使新模型更慢也如实报告。

优先消融：S预算、DA3解冻范围、decoder联合微调vs冻结；颜色失败明显时再增加位置/匹配变体。每次只改变一个关键因素，避免训练预算不同造成错误归因。

P4产物：同高斯预算的质量/速度曲线、逐场景退化、可视化、槽位分析、最终checkpoint/config与限制说明。若只有输入视角改善，不能称为NVS提升；若只有高斯更少，不能称为DA3加速。

## 9. P5：首版验证后的独立研究

- 中途丢弃patch，让后续编码真正变短；额外检查早期场景容量与local/global输出接口。
- 全局仅场景token交互、局部保留patch；需要设计跨视角信息回传。
- 二次k-means、可变S训练、按场景复杂度自适应预算，区分槽位身份与内容相似性。
- 将颜色也压入瓶颈，实现不依赖原图颜色KV的表示；单独报告信息预算和效果。

这些不并入首版实现，也不提前声称有加速或质量收益。

## 10. 当前任务清单与下一步

- [x] 本地从dev/multiview创建dev/scene_token；按用户要求迁回原ZipSplat单一工作区，ToMe残留提交到dev/local_tome保存，不带入新分支。
- [x] 固化预实验、架构、训练和评估计划，新增开发入口。
- [x] P0：远端工作区与环境核验、固定输入/target清单、相机和指标协议；详情见KMEANS_PREEXPERIMENT_PROTOCOL_ZH.md。
- [x] P1：冒烟、36组pilot、216组粗扫、108组细扫、18组固定K补点及32个独立性能配置全部完成；见KMEANS_PREEXPERIMENT_RESULTS_ZH.md。
- [x] P2：S256场景token、颜色query、训练/推理及checkpoint兼容完成；6组测试、真实Giant 1/2/8/16视角前后向、联合解冻、RGB渲染反传与FP32/BF16两路径等价通过，见SCENE_TOKEN_IMPLEMENTATION_ZH.md。
- [ ] P3：小样本验证、分阶段微调、S预算比较。
  - [x] 2026-09-07完成训练准备审计：数据全量元数据/文件存在性、首帧尺寸与部分解码、划分重叠、依赖、渲染器标记、8卡基础通信及采样/学习率口径；详见[训练准备检查](SCENE_TOKEN_TRAINING_READINESS_ZH.md)。数据派生、依赖修复和正式训练配置仍未完成。
  - [x] 随后按授权完成ZipSplat训练依赖/NumPy约束和官方gsplat fork配置；activated及总损失反传、真实S256渲染反传验证通过。抽样存储预算和跳过清单已生成，候选10337；详见[存储与环境配置](SCENE_TOKEN_STORAGE_ENVIRONMENT_ZH.md)。数据派生/teacher权重验证、train/val冻结及正式训练配置仍待完成。
  - [x] 随后按授权完成DL3DV数据pilot：冻结9820 train / 517 validation候选，保留官方test；处理8+2场景、3326帧、约269MB，生成DA3-Giant GT-pose伪深度。全图解码、2/8/16/24视角与2worker读取、固定6+8验证、真实S256全252px总损失反传通过。修复Camera/Pose堆叠递归，训练保留官方4-target采样并允许重叠，新增独立val_overrides；8项回归检查通过，optimizer更新为0。详见[数据pilot](SCENE_TOKEN_DATA_PILOT_ZH.md)。
- [ ] P4：同预算评估与研究结论。
- [ ] P5：根据首版证据选择后续方向。

2026-09-07随后完成[200步初步训练检查](SCENE_TOKEN_INITIAL_TRAINING_TEST_ZH.md)：新参数LR3e-4/原下游3e-5、已有DA3冻结、V2/B1；前100步单batch、后100步8固定batch。v3固定单样本loss下降62.9%，验证PSNR4.50→10.77dB，原版同预算14.99dB。梯度/更新/冻结参数哈希和checkpoint恢复通过，修复AMP scaler状态持久化，9项测试通过。但训练样本与验证都仍模糊，融合token有效秩约2.5（原版17.4）、GS尺度偏大；P3的高质量拟合验收仍未通过，不能把此轮记为研究成功。

2026-09-08按用户要求完成[逐层诊断](SCENE_TOKEN_LAYER_DIAGNOSIS_ZH.md)：4种状态（重放初始化、v3 step100/200、原版同K）×固定训练V2/验证V6，共8组，全部40层的attention/MLP残差、prepare、几何融合及颜色分支。hooks前后高斯参数/scene tokens/渲染逐位相同，0次优化器更新。第一个局部attention就出现公共分量占优；200步模型第一个MLP使有效秩217.85→28.28，早于全局attention。末层slot相对差异10.94%，同网络patch44.61%；原版颜色attention同样高度共享，不能将其误当新路径特有缺陷。详细指标和结论边界见新报告，权威产物`/root/multiview_compare/experiments/scene_token/layer_diagnostic_20260908_v1/`。

下一次继续先读[逐层诊断](SCENE_TOKEN_LAYER_DIAGNOSIS_ZH.md)及[初测结果](SCENE_TOKEN_INITIAL_TRAINING_TEST_ZH.md)。优先单因素比较槽位输入尺度、图像/空间锚点初始化、DA3适配范围；因问题从早层出现，不预设只解冻后部足够。以固定样本重建细节/几何覆盖验收，不能以随机噪声有效秩升高替代重建提升。颜色query/残差适配随后独立评估，S暂保持256；上述结构/训练对照尚未执行。细节重建通过后才扩数据与视角、测试模型DDP/真实吞吐，不直接启动大规模训练。每阶段把执行项、版本/配置、产物路径、失败原因和下一步写回本节及项目交接，不能把计划当成运行结果。

2026-09-08随后用户授权先验证不同位置编码，并质疑二维网格约束。已完成[一维槽位编码对照](SCENE_TOKEN_POSITION_1D_ZH.md)：保持S256一维latent、不设二维锚点，所有40层对scene Q/K施加1D RoPE（norm后、原2D RoPE前），不改V/残差或跨视角attention范围。none/shared/distinct三组从相同参数出发，各在同一书架batch训练200步，总600次更新；已有DA3冻结，原LR/完整损失不变。相对none，distinct末层秩4.60→21.31、验证PSNR8.147→10.899dB，但验证LPIPS0.7441→0.8262变差，渲染仍是大色块/尖刺，P3质量不通过。不能把高秩和PSNR提升当成精确重建成功。

最新下一步以[一维编码报告](SCENE_TOKEN_POSITION_1D_ZH.md)为准：尊重用户的一维槽位方向，二维锚定不作为必要条件或默认改动。实验adapter保留为可选对照，尚未集成公共推理API或设为默认；checkpoint需安装相应adapter后严格加载。优先验证DA3受限适配、下游融合对图像相关差异的保留，用固定样本细节和held-out LPIPS/PSNR共同验收。GVC1D实际是加法可学习latent PE+ln_pre，本轮1D RoPE不能冒称完整复现；其他编码方式、更多种子/训练步数及少数层scene-only跨视角attention仍未执行。

### 2026-09-08：随机初始化尺度对照完成

按用户要求保持随机初始化，完成[尺度对照](SCENE_TOKEN_INITIAL_SCALE_ZH.md)：同一份Gaussian槽位乘以1/4/10/16，标准差0.02/0.08/0.20/0.32；所有组固定distinct 1D RoPE、S256、原DA3冻结、相同LR/数据，各200步固定书架拟合，共800次更新。第一层attention更新/输入从4.567降到0.285，attention后cosine从0.953降到0.076，但patch注意力份额始终约58.1%，实际Q/K范数变化很小。尺度直接影响残差平衡，而非通过原始范数改变关注对象。

四组验证PSNR依次11.895/10.538/10.297/11.788dB，LPIPS0.6953/0.8164/0.7871/0.7500；均未恢复可靠细节/几何。深层与融合仍趋同，未据此改变正式初始化默认值。单种子、200步、固定绝对LR不能给出最优尺度或否定联合适配的收益。初始化前向和训练质量分开解释。

权威结果`/root/multiview_compare/experiments/scene_token/initial_scale_20260908_v1/`，本地同名results目录；新增validate/report/check_initial_scale工具，正式模型源码未改。800步关键梯度、相同LR/初始化方向/冻结hash、16组完整trace的hook等价均通过。恢复工具最初cuDNN设置不匹配，修正为Trainer的benchmark=True后，四个checkpoint新建模型strict恢复的loss/PSNR差均0；原容差未放宽，未重跑训练。四个7.37GB checkpoint留服务器，需原1D adapter，恢复时不能再次乘初始化系数。GPU已空闲，无提交/推送。下一步仍优先验证有限DA3适配与图像信息在融合/颜色分支的保留；未启动更多训练或正交初始化实验。

### 2026-09-08：重复性定位与三种子1000步拟合完成

已完成[重复性与尺度长拟合报告](SCENE_TOKEN_SCALE_REPEATS_ZH.md)：三个配对种子256/257/258，std0.02/0.08各1000步，共6000更新，已有DA3冻结、S256/不同1D槽位编码、固定书架和两验证场景不变。0.08训练PSNR/LPIPS在3/3种子改善，均值16.276→16.799dB、.5352→.4805；验证PSNR9.707→8.477、LPIPS .7819→.8483。书架横板与部分书脊已经学到，但验证过拟合明显，高质量重建及泛化仍未通过。0.08继续作为拟合候选、0.02保留对照，不修改正式默认或判定最优。

同图重复反向诊断为0更新：受控纯特征路径梯度逐位一致，渲染相关反向仍引入微扰，scene参数梯度相对差约.4–.6%；安装中的gsplat反向存在atomicAdd，尚未锁定唯一kernel，不称端到端完全确定性。cuDNN/CUBLAS受控设置和只读median例外见报告，本轮和旧200步不是仅步数不同的对照。

6000步、配对初始化、冻结哈希、相同LR、18组trace等价与六个checkpoint新建模型strict恢复均通过，恢复loss/PSNR差0，无额外optimizer续训验证。六个checkpoint共44.23GB只存服务器；轻量结果和可视化本地已归档。所有训练/检查结束，无本任务GPU进程、无提交/推送。最新下一步：以细节与验证指标评估有限DA3适配，再检验多场景输入条件化与泛化；保持单因素归因。联合微调、scene-only跨视角、正交初始化、正式DDP及全量训练均尚未执行，不自动启动。

### 2026-09-08：继续至5000步固定书架拟合（执行中）

用户授权延长训练，先单独检验训练时长。六组std0.02/0.08 × seed256/257/258恢复各自1000步完整状态，延长至5000步，新增24000次更新；2000/3000/5000保存质量指标和图像，保持已有DA3冻结与其他设置。工具continue_scale_fit.py，结果scale_longfit_20260908_v1；完成后更新结果与质量结论，不同时启动解冻、多场景或其他结构实验。

### 2026-09-08：5000步长拟合完成，更新对拟合能力的判断

[长拟合报告](SCENE_TOKEN_LONG_FIT_ZH.md)：六组从1000继续至5000，新增24000更新，已有DA3继续冻结、完整状态恢复、其余设置不变。0.02/0.08书架平均PSNR1000→5000为16.276→19.267 / 16.799→19.022；LPIPS .5352→.3392 / .4805→.3337，六组均改善；3000→5000也全部改善，尚不能认定全面收敛。横板/书脊/封面图案明显恢复，短训练低估了当前方案的固定样本拟合能力。不能再仅凭200步和相似度诊断认定结构不能学到细节。

5000步0.02在2/3种子更好，0.08在1/3更好；均值PSNR为0.02略高、LPIPS为0.08略好，无稳定最优尺度。验证场景仍严重退化，书架拟合进展不等于泛化。工程验收全部通过：24000连续更新、冻结hash/相同LR、12组trace等价、六个最终checkpoint新建模型strict恢复loss/PSNR差0；续训前完整优化器等状态一致。960张图和可切换comparison.html已归档，新增44.23GB checkpoint留服务器。训练与检查已结束，无GPU残留、无提交/推送。最新后续依据：可继续观察同配置拟合饱和点，另行测试多场景条件化；解冻/改LR/改结构分开消融。本轮未继续10000步或启动联合微调。

### 2026-09-08：用户选定std0.02工作点与下一阶段验证顺序

用户认可长拟合的初步可行性，明确后续训练采用随机初始化标准差0.02。此为当前工作点，不是已证明最优超参数；S256、distinct 1D位置编码继续保持，历史0.08结果保留，不再默认每轮并行扫尺度。0.02只在新建scene参数时使用，已有checkpoint续训不得重新初始化或乘系数。本轮仅更新研究计划，未启动训练、修改正式模型或提交/推送。

已验证的可行性边界：工程链路/梯度/保存恢复已通过；固定书架在冻结已有DA3下可持续改善并学到结构/部分细节。未证明跨场景泛化、真实新视角一致性、scene几何瓶颈的信息贡献或同预算竞争力。高cosine/低有效秩不单独作为失败标准。

后续优先级（待执行）：

1. 利用现有0.02 checkpoint做书架严格未参与训练视角的评估：预留target既未作为训练target也未作为训练context；评估context沿原2视角，不用新target RGB做TTO/相机配准。近距离插值与较远视角分开，检查遮挡/层板几何与伪影。它回答“是否只拟合监督相机”，与另一个新场景的泛化不同。
2. 使用已处理8个训练场景做多场景小规模学习，从released模型重新初始化std0.02 scene参数作为主实验，不默认从已记忆书架的checkpoint作为唯一起点；训练中变化context/target采样，提前预留同场景验证视角并从所有训练采样排除。先固定V2和冻结已有DA3，保持其他已验证配置；按实际每场景曝光次数与收敛曲线分配预算，不能把单场景5000步机械照搬成8场景总5000步。按均匀轮流8场景计算，40000总步才对应平均每场景5000次更新，这只是曝光算术，不是充分训练承诺或当前启动计划。
3. 验证区分已训练场景的新视角、未训练场景，以及视角数变化。当前训练V2而旧验证V6，同时改变场景和context数，不能将退化全部归因于场景泛化；新增未训练场景V2主对照，V6及更多视角单独报告。随着数据规模扩大再扩独立验证集，最终测试不用于选参。
4. 多场景有基本拟合后，验证图像条件化与scene贡献：相同V下切换context、检查scene/几何随内容变化，受控替换/均值化几何scene特征并保留颜色路径，比较质量及几何响应。零值或跨场景替换可能是分布外干预，只能作必要性证据之一，不能独立证明信息量/充分性；颜色KV仍读取原图，不能把重建细节全归功于scene token。
5. 达到基本多场景学习后，同输入/目标/相机/252px/指标/总GS预算对比原ZipSplat（K=VS，S256对应r256/324），区分released基线与匹配训练预算的下游适配基线。再单独测试DA3冻结vs有限解冻、S预算、多视角稳定性；scene-only跨视角等结构研究不同时引入。扩大正式训练前做真实DataLoader吞吐、显存、DDP与恢复检查。

当前建议先完成1+2+3，不把正交初始化、更多尺度或新注意力结构放在多场景验证之前。上述属于下一阶段计划，没有新的GPU实验执行结果。

### 2026-09-08：前三项验证执行中

用户已授权书架新视角、8场景动态训练及新场景V2/V6分离验证。书架0.02三种子5000步只读评估完成：监督19.267dB，未监督插值19.198dB，范围外15.217dB，0次更新；详见bookcase_novel_20260908_v2，说明存在局部新视角能力。

8场景每场景预留8帧从所有训练采样排除，512抽样验证通过；新场景V2/V6采用同跨度/同target且context嵌套。正在做8卡DDP真实20步smoke和恢复检查，通过后计划5000全局更新（global8场景，即40000场景曝光），保持std0.02/旧DA3冻结/原LR。预算口径改为同步平均batch8，不等同于B1训练40000个optimizer步。CPU帧缓存不代表正式DataLoader测速。此阶段结果尚未完成。

### 2026-09-08：前三项验证完成，后续先拆分多场景拟合问题

[详细报告](SCENE_TOKEN_MULTISCENE_ZH.md)。书架新视角通过额外teacher输入排除审计：微调6帧对应teacher输入94帧，与11张新目标交集为空；未监督近邻PSNR19.198接近监督19.267，范围外15.217且有严重伪影。支持局部新视角能力，不保证完整几何和范围外重建。

多场景原pilot的伪深度由多图teacher联合生成，最初v1主训练在各rank82步停止并留档，不作为最终结果。新建heldout_isolated_20260908_v1数据，移除10场景预留目标对非预留深度生成的影响，重算1160张深度；3326帧RGB/camera/pose与原数据一致。权威实验切换为multiscene_20260908_v2，重新从released初始化；冻结的场景和视角划分不变。

主训练完成5000次同步更新/40000场景曝光：std0.02/S256/distinct1D、已有DA3冻结、原LR不变。每rank实际5000次动态采样、3712–4018个有序context组合，2576张允许训练帧全部覆盖，每帧监督次数的场景内中位数仅55–73次；不能与固定4张书架图5000次监督等同。

5000步PSNR/LPIPS：训练参考15.032/.6143、已训练场景预留V2 14.357/.6333、未训练V2 12.985/.6367、未训练V6 12.619/.6387。已训练预留视角2500→5000提高.237dB，新V2下降.612dB但LPIPS改善.0098。图像学得粗结构/色块，细节不足、线状伪影明显；多场景高质量重建未通过，不把所有问题归为泛化，也不据此判定std0.02或S256失败。最终V6未见总体收益，但GS预算也增加三倍，仍不是同预算纯信息量消融。

工程通过：真实DDP梯度平均、连续更新/有限非零关键梯度/跨rank参数一致/冻结hash不变、全40000样本排除预留帧、最终新建模型完整model/optimizer/scheduler/scaler/计数恢复；四集合逐场景重放loss/PSNR/LPIPS差0。1280张GT/渲染与可切换comparison.html归档。主运行8×H100约34.38分钟/4.58 GPU小时，checkpoint7.372GB只存服务器；smoke/中断运行/标签重算另计。运行和恢复检查均结束，无GPU残留、无提交/推送。

最新后续顺序：保持std0.02工作点，先建议8场景固定视角受控拟合，对照动态采样以区分每帧训练量与共享模型适配困难；必要时再做有限DA3适配对照。随后继续上述第4、5项scene贡献和同预算原版验证。不要仅因这轮模糊而直接改初始化、加token或认定必须解冻；也不要只因PSNR上升就扩大正式全量训练。此处是后续建议，本轮未启动固定8batch、进一步延长、多种子、多场景联合DA3或scene-only实验。

### 2026-09-08 损失说明与公开训练方法审计

已完成用户要求的损失计算说明和 ZipSplat/GVC1D 训练方法核对，交付 `SCENE_TOKEN_LOSS_AND_TRAINING_ZH.md` 与同名 Word 文档（10页，13处原生可编辑公式）。附有有效参数、梯度规则、真实日志复算、论文/代码差异及来源。审计数据在 `results/scene_token/loss_training_review_20260908/`；Word逐页检查完成。本轮只有文档、只读源码与CPU算术检查，没有训练、改损失、提交或推送。

配置更正：当前及历史书架适配实验绕过完整模型YAML，实际继承 GaussianHead.max_scale=1.0，官方模型YAML为0.1；released权重不自动携带训练配置。故两轮损失设置相同，但不能称为完全复现官方loss配置。下轮应显式保存全部有效loss参数，阈值1.0/0.1只做单因素对照，不静默改动旧结果或与采样/解冻同时修改。

计算细节：mse_loss实际RGB L1；LPIPS为固定VGG、权重0.05；位置0.1和深度0.01。activated取context/target并集，只在Chamfer位置分支detach，loss数值仍包括所有高斯；内部另有位置越界sqrt惩罚。opacity_penalty惩罚alpha<0.01而非鼓励稀疏；RGB+D为累积深度，不是ED。BF16的LPIPS乘权重存在可复现舍入差；rank0第5000步按该语义复算total误差1.18e-9。

后续建议保留std0.02/S256及受控固定8场景拟合顺序。优先借鉴ZipSplat的训练视角/预算课程、低LR backbone联合适配，以及GVC1D的新模块适配后联合训练；另记录每项加权loss的关键模块梯度和高斯覆盖/尺度。当前不优先GAN、码率或正交loss。GVC1D为视频codec，其一维latent不能直接等价为三维场景容量；训练脚本注释步数不等于YAML自动执行步数，GAN也非所有阶段启用。上述对照均尚未执行，需后续明确训练任务。

### 2026-09-08：用户授权扩充数据后直接开始分阶段主训练

用户最新指令取代上述“先做更多受控预实验”的建议：不再追加固定8batch、初始化或max_scale消融，扩充数据后直接训练，定期保存日志、checkpoint、固定验证重建并自动更新网页；启动正常后后台独立运行，不等待训练完成。

实施方案与管理命令详见 [主训练与监控说明](SCENE_TOKEN_MAIN_TRAINING_ZH.md)。本轮 `main_20260908_v1` 准备256训练/32验证场景，沿用原冻结scene-level划分，官方测试保留；新数据放 `/data/datasets/3dvision/ZipSplat-scene-token/main_20260908_v1`，不覆盖旧pilot。训练参考图不作为held-out；泛化评估来自独立的32验证场景，每场景固定同跨度、同8target的嵌套V2/V6。

S256/std0.02/distinct1D、252px，从released重新初始化。8卡DDP、每卡1场景/4target，共50000全局更新；1–2500步旧DA3冻结，随后解冻blocks30–39，新参数/原下游/DA3峰值LR分别3e-4/3e-5/3e-6。阶段warmup250/500步后cosine；联合阶段2→3→4→5→6视角，在4500/6500/8500/10500全局步升级。AdamW moments按参数名跨阶段保留。max_scale明确设官方0.1，历史pilot为1.0，连同数据/解冻/课程一起作为新主训练配置，不解释为单因素对照。

服务器运行目录 `/root/multiview_compare/experiments/scene_token/main_20260908_v1`。首50步、每1000步和阶段末保存完整checkpoint、固定评估图；每步JSONL、每100步关键梯度、每10步状态、网页15秒刷新。保留最近3份checkpoint、阶段/里程碑及V2 PSNR/LPIPS最佳。支持STOP安全暂停和恢复；出错留日志并停止，不自动掩盖错误或重试。源码/有效配置/权重hash固定留档。

监控 [http://127.0.0.1:18766/](http://127.0.0.1:18766/) 通过本地SSH隧道读取服务器，原18765保持。实现文件为tools/scene_token下prepare_training_run、training_run_data、training_monitor、train_main、run_training、serve_training、init_main_run、launch_training及open_training_monitor.ps1。CPU状态迁移/序列化后更新一致、真实V2–V6数据预处理检查已通过；目前数据转换和等待自动启动的管理进程已运行，最终实际启动验收见PROJECT_CONTEXT后续记录。

本轮不包含全9820场景、RE10K混合、24视角、scene-only注意力、同预算基线或自动超参选择。后续优先观察固定验证曲线、训练/验证差距和重建图，再由用户决定扩大数据或改变结构；不因任务结束自动停止已授权的50k后台训练。

### 2026-09-08：主训练v2启动验收通过，后台继续

数据转换288/288完成，98332帧、tar共8,038,922,240字节（约7.49GiB），全部tar SHA256复核。随后实际训练发现1个有限但灾难性异常场景：`48aaed5a...`原始相机385–394帧平移约4.3e16，污染teacher深度至5.97e17。全批深度范围/相机量级检查仅此一例，已明确排除，不改原始数据；有效训练255、验证32。旧v1接触异常监督，在203步安全保存停止，保留全部旧日志及checkpoint，不作为主结果。

当前权威运行改为 `/root/multiview_compare/experiments/scene_token/main_20260908_v2`，数据仍引用main_20260908_v1并通过配置excluded_scenes过滤。从released重新初始化，不从旧203步续训；2500冻结期、50k预算、std0.02/S256等保持。新增归一化深度超过1e6或非有限时阻止输入、total loss非有限或绝对值>100时在反向前停止，不做loss截断。启动器torchrun参数分隔和checkpoint索引文件大小字段错误已修复，旧修正与恢复记录保留。

v2验收passed：首50全卡连续更新、各项loss有限且非异常量级、覆盖全部255训练场景；0/50固定评估三组共2304张PNG验证，50步checkpoint7,371,983,283字节重读通过；CPU优化器状态迁移/恢复后一更新一致、真实V2–V6预处理和32验证窗口通过。当前约100步仍正常运行，网页127.0.0.1:18766已切到v2并验证可切换。进程脱离SSH继续50k；阶段切换/联合V6及最终效果尚未执行验收，不预先宣称成功。操作和后续数据规模建议见主训练文档。

### 2026-09-09：主训练50k完成与模糊问题只读复核

- main_20260908_v2 正常 complete/50000，50000条global日志，无error_rank；最终checkpoint step_050000.pt约9.64GB留服务器，本次未重新加载验证。有效255训练/32验证、2500步保留171个optimizer状态解冻30–39，最后关键梯度有限非零。
- 固定验证V6在30k/40k/45k/50k的PSNR为18.76574/18.98400/19.02586/19.03445，LPIPS .487305/.477905/.476807/.476318；45k→50k仅+0.00859dB。训练两个末5k区间loss均值 .107599→.107162。第二阶段cosine至50k零LR，45k仅峰值2.77%，尾部变平不能证明容量已饱和。V2 PSNR同期16.43945→16.41429，不能说全部指标持续上升。
- 实际训练自10500步为V6；训练参考仍V2且非帧级held-out，不能直接与V6验证估计泛化差距。checkpoint best按V2维护。目视抽查中位PSNR展板与最高PSNR砖墙保存图，均有明显细节缺失；不是仅看指标即可认定重建质量通过。
- 结构复核：读取19/29/39但仅30–39解冻，早两路仍由学习scene参数改变但旧attention权重冻结；几何KV已由原版全量patch变为scene-only，因此75% kmeans对照不证明S256等价保真。上述是候选限制，未确认因果，不宣称最终token坍缩或渲染随机故障。
- 详细记录 docs/SCENE_TOKEN_50K_REVIEW_ZH.md；本地 results/scene_token/main_20260909_review/audit.json与4张既有GT/render PNG。论文450k不能机械套用：本项目从released全ZipSplat适配、数据/视角/batch/冻结及评估对齐口径不同。
- 建议但未执行：以新阶段合理非零LR续训，扩1024–2048场景并保留数据审计/隔离；若训练拟合仍差优先排查多层适配和几何/颜色对应。暂不全局改scene-only，可后续少数后期层研究。当前用户仅询问原因和方向，本次无续训、数据转换、模型修改、提交或推送；不因交接自动重启。

### 2026-09-09：用户接受建议，扩充1024场景并部署50k后续训练

- 用户“好的”接受上一轮建议，本轮据此落实下一阶段。新运行 vllm1:/root/multiview_compare/experiments/scene_token/continue_20260909_v1；从main_20260908_v2/checkpoints/step_050000.pt严格恢复模型、按名字保留全部旧AdamW状态及scaler/RNG，新阶段重设scheduler。计划新增100k更新，累计50k→150k；固定V6/global8/4target、S256/std0.02来源/1D/252px/损失/完整patch+scene全局交互不变。
- 解冻18–39；新增18–29峰值LR1e-6，已有30–39为3e-6，下游3e-5，scene/颜色query1e-4。1000新增步LR warmup，随后cosine至各组峰值10%（非零）。多路特征适配、数据、LR同时变，是下一主阶段而非因果消融。保留首50050、每2000、里程碑、最近3份与V6最佳checkpoint；新增相同8训练参考的V6图，仍非held-out。
- 新数据 /data/datasets/3dvision/ZipSplat-scene-token/expanded_20260909_v1，源筛查选1024train+原32val/360145帧。相机有限/齐次/旋转/轨迹量级/跳变及图片存在检查排除3场景：48aaed5a...、0888cdee...、98341043...；复用已校验tar287（255train+32val），新增769，旧数据未改。后续逐tar哈希和深度范围/相对轨迹尺度审计；验证缺失/变化或有效训练低于97%阻止启动。不能把源筛查通过当作最终数据ready。
- 已部署 prepare_continuation_data.py、init_continuation_run.py、continuation_schedule.py、check_continuation_runtime.py，给train_main/run_training增加显式跨运行初始化、名字迁移状态、新阶段schedule、V6参考和按V6保留。旧50k快照不改，新运行冻结159个Python文件hash；以后不得编辑这些文件后不更新/重建运行来源。CPU小模型已通过名字LR分组、moments迁移、新参数空状态、调度下限、恢复下一更新相同及错误target拒绝；真实大模型恢复尚待数据完成后执行。
- 首次更新前会重放原50k的train_probe/V2/V6相同场景和视图，PSNR逐场景差<.01、LPIPS/total差<.001才通过。第50050 checkpoint关键参数回读，blocks18/29/39、scene、颜色query、head梯度有限非零检查。数据ready后管理器先CPU真实采样检查再自动启动8卡训练，遇错停止并保留日志，不自动掩盖重试。
- 数据管理PID14716，8worker15128–15135；新训练管理PID15093，viewer15092；新URL仍127.0.0.1:18766，旧viewer切18767/PID15079，已建本地两隧道。网页真实浏览器核对V6默认和新增训练参考V6、保存频率、旧结果入口。导入父50k的1152张参考PNG，后续由实际重放覆盖；不要误称这批图是新训练结果。
- 当前进度快照295/1056（其中287复用），8teacher worker已正常生成新增场景，0转换失败；新训练仍preparing_data，尚无新增优化器更新。数据完成/大模型恢复/50050验收及150k效果均待后台实际执行。旧main_v2完整结果保留；不因交接重复启动转换或管理器，也不自动停止已授权后台任务。
- 操作与完整口径见 ZipSplat/docs/SCENE_TOKEN_CONTINUATION_ZH.md；数据总日志 /root/multiview_compare/logs/scene_token_expand_20260909_v1.log，新运行pipeline/status会自动更新。无提交/推送，其他修改保留。open_training_monitor.ps1默认重连18766与18767。

### 2026-09-09：scene-token 代码提交归档

用户要求将当前代码提交到远程。归档范围包含此前未提交的scene-token模型/颜色分支/检查点支持、实验与数据工具、后台训练和50k续训、测试及Markdown说明；数据、权重、结果图片、日志和生成的Word文件不纳入Git。本地70个待提交Python文件与vllm1实际文件按LF规范化哈希全部一致。提交前服务器隔离CUDA运行unit tests：9项中8项通过、1项CUDA测试跳过；本轮不新增GPU测试或改变运行中的数据/训练。推送目标为用户fork Glooow1024/ZipSplat 的dev/scene_token，实际commit与推送结果见父目录PROJECT_CONTEXT。
